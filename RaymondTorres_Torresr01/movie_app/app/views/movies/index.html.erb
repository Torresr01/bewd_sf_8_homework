<center><h1> <style=border:1px solid red> RetFLIX</h1></center>
<center><p> BETTER THAN IMDB </p></center>

<b><p class="small"><align = "left">
Search for a movie: 
<%= link_to 'Search Collection', movies_search_path %>
</b></p>

<align = "left"><b><p > Know a movie we're missing? Add it to our collection: <%= link_to 'Add Movie', new_movie_path %></b></p>

<table>
  <thead>
      <th>Title</th>
      <th>Description</th>
      <th>Year Released</th>
      <th> </th>
    </tr>
  </thead>

  <tbody>
  <% @movies.each do |movie| %>
    <tr align="left">
        
      <td><%= movie.title %></td>
      <td><%= movie.description %></td>
      <td align = "center"><%= movie.year_released %></td>
      <td><%= link_to 'edit', edit_movie_path(movie) %></td>
    </tr>
  <% end %>  
  </tbody>
</table>
<br>

